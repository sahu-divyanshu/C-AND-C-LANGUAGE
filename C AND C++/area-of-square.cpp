#include<iostream>
using namespace std;
int main(){
    int width(0);
    int length(0);
    cout << "enter the width ";
    cin>>width;

    cout<<"enter the length";
    cin>>length;

    cout<<"the area is"<< length *width << endl;
    return 0;

}
